const numeroCliente = parseInt(prompt("Introduce tu número de cliente:"));
if (numeroCliente === 1000) {
  alert("¡Ganaste un premio!");
} else {
  alert(`${numeroCliente} Lo sentimos, sigue participando`);
}
